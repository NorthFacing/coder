package ${packageName}.${className_x}.service;

import ${packageName}.base.service.BaseService
import ${packageName}.${className_x}.entity.${className_d}

/**
 * ${className_d}Service
 * @since v0.0.1
 * @author Bob.Zhu
 * @Date ${.now}
 */
interface ${className_d}Service : BaseService<${className_d}>