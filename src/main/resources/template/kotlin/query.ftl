package ${packageName}.${className_x}.entity

import ${packageName}.base.entity.BaseQuery

/**
 * ${className_d}
 * @since v0.0.1
 * @author Bob.Zhu
 * @Date ${.now}
 */
class ${className_d}Query : BaseQuery<${className_d}>()
